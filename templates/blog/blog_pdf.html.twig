<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ blog.title }}</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            margin: 20px; 
            text-align: center;
        }
        h1 { 
            text-align: center; 
            font-size: 22px; 
            color: #333;
        }
        .content { 
            margin-top: 20px; 
            text-align: justify;
        }
        .meta {
            font-size: 14px;
            color: #555;
            margin-top: 10px;
        }
        .blog-image {
            max-width: 100%;
            height: auto;
            margin: 20px auto;
            display: block;
            border-radius: 5px;
        }
        .category {
            font-size: 16px;
            font-weight: bold;
            color: #007BFF;
            margin-top: 10px;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #888;
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>{{ blog.title }}</h1>

    {% if imagePath %}
        <img src="{{ imagePath }}" alt="Image du blog" class="blog-image">
    {% endif %}

    {% if blog.Category is not empty %}
        <p class="category">
            Catégorie :
            {% for cat in blog.Category %}
                {{ cat.name }}{% if not loop.last %}, {% endif %}
            {% endfor %}
        </p>
    {% else %}
        <p class="category">Catégorie : Non spécifiée</p>
    {% endif %}

    <p class="meta"><strong>Date :</strong> {{ blog.createdAt|date('d/m/Y') }}</p>

    <div class="content">
        {{ blog.content|nl2br }}
    </div>

    <div class="footer">
        <p>Généré par SanareNovo | {{ "now"|date("d/m/Y H:i") }}</p>
    </div>
</body>
</html>
